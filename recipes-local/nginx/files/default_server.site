upstream django {
    server 127.0.0.1:8000;
}

server {
    listen      80;
    server_name localhost;
    charset     utf-8;

    root /srv/www;
    index index.html;

    location /rest {
       proxy_pass http://127.0.0.1:8000;
    }

    client_max_body_size 75M;
}
